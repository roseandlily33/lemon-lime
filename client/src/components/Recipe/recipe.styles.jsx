import styled from 'styled-components';

export const RecipeCont = styled.div`
    box-shadow: 2px 5px 30px 5px lightgrey;
    background-color: ${({theme}) => theme.colors.white};
    height: 200px;
    width: auto;
    padding: 1em;
    border-radius: 5px;
    margin-top: 1em;

`;